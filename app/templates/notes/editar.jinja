{% extends "base.jinja" %}

{% block title %}Editar Nota{% endblock title %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <h1 class="text-center mb-4">Editar Nota</h1>
            <div class="card">
                <div class="card-body">
                    <form method="POST" action="{{ url_for('notes.actualizar_nota', id=nota.id) }}">
                        {{ form.hidden_tag() }}

                        <!-- Campo para el Título -->
                        <div class="form-group">
                            <label for="titulo">Título</label>
                            {{ form.titulo(class_='form-control', value=nota.titulo) }}
                        </div>

                        <!-- Campo para el Contenido -->
                        <div class="form-group">
                            <label for="contenido">Contenido</label>
                            {{ form.contenido(class_='form-control', value=nota.contenido) }}
                        </div>

                        <!-- Botones -->
                        <div class="d-flex justify-content-between">
                            <!-- Botón de Guardar Cambios -->
                            {{ form.submit(class_='btn btn-primary') }}

                            <!-- Botón de Cancelar -->
                            <a href="{{ url_for('notes.notes_list') }}" class="btn btn-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock body %}
